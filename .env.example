# Google Apps Script: Deploy > New deployment > Web app (Execute as: Me, Who has access: Anyone).
# Must be the /exec URL — NOT docs.google.com/.../edit and NOT …/macros/library/d/… (library docs only).
# Contact.jsx POSTs JSON { fullName, email, phone, message } (see scripts/google-apps-script-contact-webapp.gs).
VITE_CONTACT_FORM_SCRIPT_URL=
